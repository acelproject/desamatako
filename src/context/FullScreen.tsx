// import { createContext, useState } from "react";

// type FullScreenContextType = true | false;

// const FullScreenContext = createContext<FullScreenContextType>(false);

// const DarkModeContextProvider = ({ children }: any) => {
//   const [isFullScreen, setIsFullScreen] = useState<FullScreenContextType>(false);
//   return (
//     <FullScreenContext.Provider value={{isFullScreen,setIsFullScreen}}>
//       {children}
//     </FullScreenContext.Provider>
//   );
// };
